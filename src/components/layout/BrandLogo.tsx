import React from 'react';
import svgPaths from "../../imports/svg-nb4s686ca5";

interface BrandLogoProps {
  scale?: number;
  className?: string;
}

export function BrandLogo({ scale = 1, className = "" }: BrandLogoProps) {
  const width = 338.4 * scale;
  const height = 88.971 * scale;
  
  return (
    <div className={`relative ${className}`} style={{ width: `${width}px`, height: `${height}px` }}>
      <div className="absolute flex h-full items-center justify-center" style={{ left: `${164.09 * scale}px`, top: `${9.13 * scale}px`, width: `${67.669 * scale}px`, height: `${88.971 * scale}px` }}>
        <div className="flex-none rotate-[-165.43deg]">
          <div className="relative" style={{ height: `${79.098 * scale}px`, width: `${49.358 * scale}px` }}>
            <div className="absolute inset-[-0.29%_-0.29%_-0.58%_-0.51%]">
              <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 49.7505 79.7901">
                <path d={svgPaths.p1a4e9c40} fill="#7B9669" id="Vector 26" stroke="#7B9669" strokeWidth="0.5" />
              </svg>
            </div>
          </div>
        </div>
      </div>
      <div className="absolute flex h-full items-center justify-center" style={{ left: `${182.35 * scale}px`, top: `${-23.03 * scale}px`, width: `${68.149 * scale}px`, height: `${89.178 * scale}px` }}>
        <div className="flex-none rotate-15">
          <div className="relative" style={{ height: `${79.098 * scale}px`, width: `${49.358 * scale}px` }}>
            <div className="absolute inset-[-0.29%_-0.29%_-0.58%_-0.51%]">
              <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 49.7505 79.7901">
                <path d={svgPaths.p1a4e9c40} fill="#404F3C" id="Vector 30" stroke="#404F3C" strokeWidth="0.5" />
              </svg>
            </div>
          </div>
        </div>
      </div>
      <div className="absolute bg-[#404f3c] border border-[#404f3c] border-solid" style={{ height: `${5.744 * scale}px`, left: `${112.4 * scale}px`, top: `${11.43 * scale}px`, width: `${28.718 * scale}px` }} />
      <div className="absolute bg-[#404f3c] border border-[#404f3c] border-solid" style={{ height: `${40.205 * scale}px`, left: `${123.89 * scale}px`, top: `${17.17 * scale}px`, width: `${5.744 * scale}px` }} />
      <div className="absolute bg-[#404f3c] border border-[#404f3c] border-solid" style={{ height: `${5.744 * scale}px`, left: `${112.4 * scale}px`, top: `${57.38 * scale}px`, width: `${28.718 * scale}px` }} />
      <div className="absolute bg-[#404f3c] border border-[#404f3c] border-solid" style={{ height: `${51.692 * scale}px`, left: `${146.86 * scale}px`, top: `${11.43 * scale}px`, width: `${5.744 * scale}px` }} />
      <div className="absolute bg-[#404f3c] border border-[#404f3c] border-solid" style={{ height: `${51.692 * scale}px`, left: `${175.58 * scale}px`, top: `${11.43 * scale}px`, width: `${5.744 * scale}px` }} />
      <div className="absolute" style={{ height: `${51.692 * scale}px`, left: `${146.86 * scale}px`, top: `${11.43 * scale}px`, width: `${34.462 * scale}px` }}>
        <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 34.4615 51.6923">
          <path d={svgPaths.p39132c0} fill="#404F3C" id="Rectangle 10" stroke="#404F3C" />
        </svg>
      </div>
      <div className="absolute" style={{ height: `${51.692 * scale}px`, left: `${227.27 * scale}px`, top: `${11.43 * scale}px`, width: `${22.974 * scale}px` }}>
        <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 22.9744 51.6923">
          <path d={svgPaths.p2833a00} fill="#7B9669" id="Rectangle 12" />
        </svg>
      </div>
      <div className="absolute" style={{ height: `${51.692 * scale}px`, left: `${244.5 * scale}px`, top: `${11.43 * scale}px`, width: `${22.974 * scale}px` }}>
        <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 22.9744 51.6923">
          <path d={svgPaths.p2e171800} fill="#7B9669" id="Rectangle 13" />
        </svg>
      </div>
      <div className="absolute" style={{ height: `${5.744 * scale}px`, left: `${233.02 * scale}px`, top: `${40.15 * scale}px`, width: `${28.718 * scale}px` }}>
        <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 28.7179 5.74359">
          <path d={svgPaths.p2ef28700} fill="#7B9669" id="Rectangle 14" />
        </svg>
      </div>
      <div className="absolute bg-[#7b9669]" style={{ height: `${51.692 * scale}px`, left: `${273.22 * scale}px`, top: `${11.43 * scale}px`, width: `${5.744 * scale}px` }} />
      <div className="absolute bg-[#7b9669]" style={{ height: `${5.744 * scale}px`, left: `${278.96 * scale}px`, top: `${57.38 * scale}px`, width: `${22.974 * scale}px` }} />
      <div className="absolute" style={{ height: `${51.692 * scale}px`, left: `${307.68 * scale}px`, top: `${11.43 * scale}px`, width: `${5.744 * scale}px` }}>
        <svg className="block size-full" fill="none" preserveAspectRatio="none" viewBox="0 0 5.74359 51.6923">
          <path d="M0 0H5.74359V51.6923H0V0Z" fill="#7B9669" id="Rectangle 17" />
        </svg>
      </div>
      <div className="absolute bg-[#7b9669]" style={{ height: `${5.744 * scale}px`, left: `${307.68 * scale}px`, top: `${11.43 * scale}px`, width: `${28.718 * scale}px` }} />
      <div className="absolute bg-[#7b9669]" style={{ height: `${5.744 * scale}px`, left: `${307.68 * scale}px`, top: `${34.4 * scale}px`, width: `${28.718 * scale}px` }} />
      <div className="absolute bg-[#7b9669]" style={{ height: `${5.744 * scale}px`, left: `${307.68 * scale}px`, top: `${57.38 * scale}px`, width: `${28.718 * scale}px` }} />
    </div>
  );
}
